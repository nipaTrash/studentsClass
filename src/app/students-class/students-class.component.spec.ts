/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { StudentsClassComponent } from './students-class.component';

describe('StudentClassComponent', () => {
  let component: StudentsClassComponent;
  let fixture: ComponentFixture<StudentsClassComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ StudentsClassComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(StudentsClassComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */